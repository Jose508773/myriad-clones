# The core FastAPI framework to build the API
fastapi
# The server required by Vercel to run the Python ASGI app
uvicorn
# Required by FastAPI to serve static files
aiofiles
# Supabase Python client — pinned to a stable version
supabase==2.3.5